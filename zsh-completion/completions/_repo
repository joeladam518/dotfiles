#compdef repo

_repo()
{
    local -a opts
    if command -v dotfiles >/dev/null 2>&1; then
        opts=( $(dotfiles repos --list-keys --sep=" ") )
    else
        opts=()
    fi

    compadd -a opts
}

_repo "$@"
