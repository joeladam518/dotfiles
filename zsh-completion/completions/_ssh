#compdef ssh

_ssh()
{
    local -a opts
    if [[ -f "${HOME}/.ssh/config" && -r "${HOME}/.ssh/config" ]]; then
        opts=( $(grep '^Host' ~/.ssh/config 2>/dev/null | grep -v '[?*]' | cut -d ' ' -f 2-) )
    else
        opts=()
    fi
    compadd -a opts
}

_ssh "$@"
